import React from "react";
import "../../styles/Loading.css";

const Loading = () => {
    return (
        <div id="loadingContainer">
            <div id="loader"></div>
        </div>
    );
};

export default Loading;
